const config = {
    development: {
        username: 'postgres',
        password: 'postgres',
        database: 'asta_snap',
        host: '127.0.0.1',
        dialect: 'postgres',
        }
};

export = config;