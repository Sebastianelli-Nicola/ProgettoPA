const config = {
    development: {
        username: 'postgres',
        password: 'postgres',
        database: 'snap_auction',
        host: '127.0.0.1',
        dialect: 'postgres',
        }
};

export = config;